name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Install native scanner dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libclamav-dev libpcap-dev libunicorn-dev libyara-dev
      - name: Formatting
        run: test -z "$(gofmt -l .)"
      - name: Vet
        run: go vet ./...
      - name: Test
        run: go test -race ./...
      - name: Build
        run: go build ./cmd/darkscan ./cmd/darkscand ./cmd/darkscan-carve ./cmd/darkscan-stego
